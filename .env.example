# Variables de Entorno Requeridas

# Clave de API para Inteligencia Artificial (Gemini)
GEMINI_API_KEY=tu_clave_aqui

# Entorno (development | production)
NODE_ENV=development

# Puerto del servidor (Hostinger lo asigna automáticamente, pero puedes forzarlo)
PORT=3000
